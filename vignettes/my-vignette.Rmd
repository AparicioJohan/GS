---
title: "How to start"
author: "Aparicio Johan"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{How to start}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#"
)
```

## Loading libraries

```{r setup, warning=F,message=F}
library(tidyverse)
library(GS)
library(sommer)
```


## Genotypic information

```{r, warning=F,message=F}
data(DT_cpdata)

geno <- GT_cpdata
geno[1:5,1:3]
```

## Vector with genotype names
```{r, warning=F,message=F}
samp <- rownames(GT_cpdata)
samp[1:5]
```

## Phenotypic information
```{r, warning=F,message=F}
phen <- DT_cpdata
head(phen,3)
```


## Cross validation
```{r, warning=F}
tmp <- crossGP(geno,samp,phen,prior = "sommer", niter=5,testporc = 0.3,traits = names(phen)[5:6])
```

## 

```{r}
ggplot(tmp$data, aes(x=trait,y=corr))+geom_boxplot()
```



